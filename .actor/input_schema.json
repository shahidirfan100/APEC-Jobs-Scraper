{
  "title": "APEC Jobs Scraper",
  "description": "Extract job listings from APEC.fr. Provide a search URL or use keyword + location/department.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrl": {
      "title": "Start URL",
      "type": "string",
      "description": "APEC search URL to scrape. If set, overrides keyword/location/department.",
      "editor": "textfield",
      "example": "https://www.apec.fr/candidat/recherche-emploi.html/emploi?lieux=27&motsCles=ingenieur"
    },
    "keyword": {
      "title": "Search Keywords",
      "type": "string",
      "description": "Job title or skills to search (e.g., 'ingenieur logiciel', 'chef de projet').",
      "editor": "textfield",
      "prefill": "ingenieur",
      "example": "ingenieur logiciel"
    },
    "location": {
      "title": "City or Region",
      "type": "string",
      "description": "City or region to search (e.g., 'Paris', 'Lyon'). Overrides department if set.",
      "editor": "textfield",
      "example": "Paris"
    },
    "department": {
      "title": "Department Code",
      "type": "string",
      "description": "French department number (e.g., '75' for Paris, '27' for Eure, '69' for Rhône).",
      "editor": "textfield",
      "example": "27"
    },
    "collectDetails": {
      "title": "Collect Full Job Details",
      "type": "boolean",
      "description": "Visit each job detail page to extract complete descriptions and additional information.",
      "editor": "checkbox",
      "default": true
    },
    "results_wanted": {
      "title": "Maximum Jobs to Extract",
      "type": "integer",
      "description": "Maximum number of job listings to collect.",
      "minimum": 1,
      "default": 20,
      "editor": "number"
    },
    "max_pages": {
      "title": "Maximum Pages",
      "type": "integer",
      "description": "Maximum number of result pages to process.",
      "minimum": 1,
      "editor": "number",
      "default": 3
    },
    "maxConcurrency": {
      "title": "Max Concurrency",
      "type": "integer",
      "description": "Maximum concurrent requests (tune speed vs blocking risk).",
      "minimum": 1,
      "default": 3,
      "editor": "number"
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Recommended: Use Apify Proxy (residential or datacenter) to avoid blocking.",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true,
        "apifyProxyGroups": ["RESIDENTIAL"]
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
