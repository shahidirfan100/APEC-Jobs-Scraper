{
  "title": "APEC Jobs Scraper",
  "description": "Extract job listings from APEC.fr, France's leading executive job board. Search by keyword, department, contract type, and remote work options. Supports both API and HTML parsing methods.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrl": {
      "title": "Start URL",
      "type": "string",
      "description": "Start scraping from a specific APEC search URL. If provided, this overrides keyword/department settings.",
      "editor": "textfield",
      "example": "https://www.apec.fr/candidat/recherche-emploi.html/emploi?lieux=27"
    },
    "keyword": {
      "title": "Search Keywords",
      "type": "string",
      "description": "Job title or skills to search for (e.g., 'Ingénieur Logiciel', 'Chef de Projet', 'Développeur').",
      "editor": "textfield",
      "prefill": "ingénieur",
      "example": "ingénieur logiciel"
    },
    "department": {
      "title": "Department Code",
      "type": "string",
      "description": "French department number (e.g., '75' for Paris, '27' for Eure, '69' for Rhône).",
      "editor": "textfield",
      "example": "27"
    },
    "contractType": {
      "title": "Contract Types",
      "type": "array",
      "description": "Filter by contract types. Common codes: 143684 (Entreprise), 143685 (Cabinet), 143686 (Agence), 143687 (SSII/ESN).",
      "editor": "json",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "remoteWork": {
      "title": "Remote Work Options",
      "type": "array",
      "description": "Filter by remote work availability (télétravail).",
      "editor": "json",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "collectDetails": {
      "title": "Collect Full Job Details",
      "type": "boolean",
      "description": "Visit each job detail page to extract complete descriptions and additional information.",
      "editor": "checkbox",
      "default": true
    },
    "useApi": {
      "title": "Use API Method",
      "type": "boolean",
      "description": "Try to use APEC's JSON API first before falling back to HTML parsing (faster and more reliable).",
      "editor": "checkbox",
      "default": true
    },
    "results_wanted": {
      "title": "Maximum Jobs to Extract",
      "type": "integer",
      "description": "Maximum number of job listings to collect (set to high number for all results).",
      "minimum": 1,
      "default": 100,
      "editor": "number"
    },
    "max_pages": {
      "title": "Maximum Pages",
      "type": "integer",
      "description": "Maximum number of result pages to process (safety limit).",
      "minimum": 1,
      "editor": "number",
      "default": 20
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Recommended: Use Apify Proxy (residential or datacenter) to avoid blocking.",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true,
        "apifyProxyGroups": ["RESIDENTIAL"]
      }
    }
  },
  "required": [],
  "additionalProperties": false
}